//This launch.json file is made to work with extensions such as 'cdt-gdb-vscode'
{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "gdb",
            "request": "launch",
            "name": "GDB (bah) launch",
            "program": "${workspaceFolder}/.tmp_bah/debug"
        }
    ]
}
